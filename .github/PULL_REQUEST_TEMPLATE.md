## What changed
- 

## Modules affected
<!-- List each module and whether its version changed. Example:
- ManageEngine.ServiceDesk.OnPrem.Core — x.y.z → x.y.z+1
- ManageEngine.ServiceDesk.OnPrem.Requests — unchanged
-->
- 

## Testing
<!-- Describe what was run and what passed (e.g. `./build.ps1` output, integration test env). -->


## Checklist

- [ ] `./build.ps1` passes (all tasks)
- [ ] New public functions have comment-based help and at least one Pester unit test
- [ ] Version numbers bumped as needed in `.psd1` files
- [ ] `RequiredModules` constraints updated in dependent `.psd1` files if a sub-module version changed
- [ ] PR title follows `[Module] Past-tense description` format
